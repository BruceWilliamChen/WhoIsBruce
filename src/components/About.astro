<article class="about-card">

  <header class="about-header">
    <h1>About</h1>
    <p class="about-lede">Hi, I'm Bruce — software engineer, builder, perpetual tinkerer.</p>
  </header>

  <section class="about-section">
    <h2>Right now</h2>
    <p>Deep in the AI wave — not just using it, but thinking hard about how agents can reshape the way engineers work. If a tool can amplify what I can build, I want to understand it inside out.</p>
  </section>

  <section class="about-section">
    <h2>Outside of work</h2>
    <div class="about-grid">
      <div class="mini-card">
        <h3><span class="emoji">🏊</span> Swimming</h3>
        <p>Regular laps. Best way to clear the head.</p>
      </div>
      <div class="mini-card">
        <h3><span class="emoji">🎬</span> Movies</h3>
        <p>Chronic moviegoer. Big screen whenever possible.</p>
      </div>
      <div class="mini-card">
        <h3><span class="emoji">✍️</span> Writing</h3>
        <p>Novels, just for myself. No agenda, no readers required.</p>
      </div>
      <div class="mini-card">
        <h3><span class="emoji">🫧</span> Perfumery</h3>
        <p>Sizeable collection. Quietly dreaming of professional perfumer training someday.</p>
      </div>
    </div>
  </section>

  <section class="about-section">
    <h2>This site</h2>
    <div class="about-grid">
      <div class="mini-card">
        <h3><span class="emoji">🏠</span> This space</h3>
        <p>My corner of the internet. Built it because I wanted a space that felt like me, not a template.</p>
      </div>
      <div class="mini-card">
        <h3><span class="emoji">📝</span> The blog</h3>
        <p>Where I think out loud — no fixed theme, no schedule. Could be a deep dive into AI agents, a fragrance review, notes on a novel I'm reading. Whatever's worth writing down.</p>
      </div>
    </div>
  </section>

</article>

<style>
  .about-card {
    position: absolute;
    inset: 2rem 2.5rem;
    overflow-y: auto;
    background: #FAF7F0;
    border-radius: 14px;
    padding: 3rem 4rem 4rem;
    box-shadow:
      0 24px 48px rgba(0,0,0,0.10),
      0 8px 16px rgba(0,0,0,0.06),
      0 2px 4px rgba(0,0,0,0.04);
    color: #2C2826;
    font-family: system-ui, -apple-system, "Segoe UI", sans-serif;
    line-height: 1.7;
  }

  .about-card::-webkit-scrollbar { width: 8px; }
  .about-card::-webkit-scrollbar-track { background: transparent; }
  .about-card::-webkit-scrollbar-thumb {
    background: rgba(0,0,0,0.18);
    border-radius: 4px;
  }
  .about-card::-webkit-scrollbar-thumb:hover {
    background: rgba(0,0,0,0.32);
  }

  /* ── Header ── */
  .about-header {
    margin-bottom: 2.5rem;
  }

  .about-header h1 {
    font-size: 2.4rem;
    font-weight: 700;
    color: #1A1714;
    letter-spacing: -0.01em;
    margin-bottom: 0.6rem;
  }

  .about-lede {
    font-size: 1.15rem;
    color: #4A4540;
    line-height: 1.6;
    max-width: 56ch;
  }

  /* ── Sections ── */
  .about-section {
    margin-bottom: 2.5rem;
  }

  .about-section:last-child {
    margin-bottom: 0;
  }

  .about-section h2 {
    font-size: 0.78rem;
    font-weight: 700;
    color: #6B6358;
    text-transform: uppercase;
    letter-spacing: 0.12em;
    margin-bottom: 0.85rem;
  }

  .about-section > p {
    font-size: 1.02rem;
    color: #2C2826;
    max-width: 64ch;
  }

  /* ── Items grid (no boxes) ── */
  .about-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    column-gap: 3rem;
    row-gap: 1.6rem;
  }

  .mini-card {
    background: transparent;
    border: none;
    border-radius: 0;
    padding: 0;
  }

  .mini-card h3 {
    font-size: 1rem;
    font-weight: 600;
    color: #1A1714;
    margin-bottom: 0.3rem;
  }

  .mini-card .emoji {
    margin-right: 0.4rem;
  }

  .mini-card p {
    font-size: 0.95rem;
    color: #4A4540;
    line-height: 1.6;
  }
</style>
